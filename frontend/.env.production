# Build de producción: el frontend se sirve desde su propio origen y llama
# a la API de Railway directo (con CORS -- ver VRIDIK_ALLOWED_ORIGINS en el
# backend, que debe incluir el origen donde se despliegue este frontend).
VITE_API_BASE=https://vridik-api-production.up.railway.app
