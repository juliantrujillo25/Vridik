# Vridik — dependencias de testing (Sprint S3, actualizado en S4 semana 4-6)
# Instalar con: pip install -r requirements-test.txt

pytest==8.3.3
pytest-asyncio==0.24.0
asyncpg==0.29.0
bcrypt==4.2.0
PyJWT==2.9.0
httpx==0.27.2
ruff==0.6.9

# Añadidos en S4 (JuliX real + api/julix_endpoint.py)
anthropic==0.116.0
fastapi==0.139.0
pydantic==2.13.4

# Añadido en S12 (core/totp_2fa.py)
pyotp==2.9.0

# Añadido en S12 (conexión HTTP del 2FA, api/auth_endpoint.py)
cryptography==42.0.5
qrcode[pil]==7.4.2

# Añadido en S7 (core/storage.py backend r2)
boto3==1.35.36

# Faltaba desde S3/S4 — core/auth.py usa python-jose (JWT) y passlib
# (hashing de contraseñas); su ausencia rompía la RECOLECCIÓN de pytest en
# CI (ModuleNotFoundError en 8 módulos de test), dejando el gate del 90%
# en 0% en cada run desde S4.
python-jose==3.3.0
passlib[bcrypt]==1.7.4

# Faltaba desde S10/S11 — tests/test_pdf_export.py llama a
# julix.pdf_export.generar_pdf(), que requiere reportlab (import perezoso).
reportlab==4.2.5

# S3-GAP-01 (AUDITORIA_PARA_CLAUDE.md): gate de cobertura ≥60% en CI.
pytest-cov==5.0.0
